{
  "name": "gatekit",
  "version": "0.0.0",
  "type": "module",
  "bin": { "gatekit": "dist/index.js" },
  "files": ["dist", "registry", "registry.json"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test 'test/**/*.test.ts'",
    "coverage": "c8 --reporter=text --reporter=json-summary --src src node --import tsx --test 'test/**/*.test.ts'"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "@types/node": "^25.9.3", "c8": "^10.1.3", "tsx": "^4.22.4", "typescript": "^5.9.3" }
}
